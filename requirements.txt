# Root-level requirements.txt for Railway deployment
# This file ensures Railway detects this as a Python project
# and installs backend dependencies

# Backend dependencies
-r backend/requirements.txt

# Explicitly include PyJWT to ensure it's installed
PyJWT>=2.8.0
cryptography>=41.0.0
requests>=2.31.0
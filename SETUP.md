# Инструкция по установке и запуску Semantle

## Шаг 1: Подготовка окружения

### Установка Python (если не установлен)
1. Скачайте Python 3.8+ с [python.org](https://www.python.org/downloads/)
2. При установке отметьте "Add Python to PATH"
3. Проверьте установку: `python --version`

### Установка Node.js (если не установлен)
1. Скачайте Node.js 16+ с [nodejs.org](https://nodejs.org/)
2. Проверьте установку: `node --version` и `npm --version`

## Шаг 2: Установка Backend

```bash
# Перейти в директорию проекта
cd Semantle

# Перейти в backend
cd backend

# Создать виртуальное окружение
python -m venv venv

# Активировать виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

## Шаг 3: Установка Frontend

```bash
# Из корневой директории проекта
cd frontend

# Установить зависимости
npm install
```

## Шаг 4: Запуск приложения

### 🎯 Быстрый способ (одной командой):

После установки всех зависимостей, вы можете запустить игру одной командой:

```bash
# Из корневой директории проекта
python start.py
```

Это автоматически запустит оба сервера (backend и frontend). Для остановки нажмите `Ctrl+C`.

### Альтернативный способ (вручную):

Если вы хотите запускать серверы отдельно:

**Терминал 1 - Backend:**
```bash
cd backend
# Убедитесь, что виртуальное окружение активировано
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

python main.py
```

Backend должен запуститься на `http://localhost:8000`

**Терминал 2 - Frontend:**
```bash
cd frontend
npm run dev
```

Frontend должен запуститься на `http://localhost:3000`

## Шаг 5: Открыть в браузере

Откройте браузер и перейдите на `http://localhost:3000`

## Проверка работы

1. Backend API доступен: `http://localhost:8000` - должна быть страница с `{"message": "Semantle API is running"}`
2. Frontend доступен: `http://localhost:3000` - должна открыться игра
3. Попробуйте ввести слово из словаря и нажать "Guess"

## Решение проблем

### Ошибка "Module not found"
- Убедитесь, что все зависимости установлены
- Для backend: `pip install -r requirements.txt`
- Для frontend: `npm install`

### Порт занят
- Backend: измените порт в `backend/main.py` (строка с `uvicorn.run`)
- Frontend: измените порт в `frontend/vite.config.js`

### CORS ошибки
- Убедитесь, что backend запущен на порту 8000
- Проверьте настройки CORS в `backend/main.py`

### Слова не находятся
- Убедитесь, что `words.json` находится в корне проекта
- Проверьте, что слова в нижнем регистре

## Улучшение качества игры (опционально)

Для лучших результатов с embeddings:

1. **Скачайте предобученную модель Word2Vec:**
   ```bash
   # Создать директорию
   mkdir -p backend/models
   
   # Скачать модель (пример)
   # Google News Word2Vec: https://drive.google.com/file/d/0B7XkCwpI5KDYNlNUTTlSS21pQmM
   ```

2. **Или используйте GloVe:**
   ```bash
   wget https://nlp.stanford.edu/data/glove.6B.zip
   unzip glove.6B.zip
   # Поместить в backend/models/
   ```

3. **Обновите `embeddings_manager.py`** для загрузки модели

---

**Готово! Наслаждайтесь игрой! 🎮**

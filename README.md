# Semantle - Semantic Word Guessing Game

Полнофункциональная игра Semantle на английском языке, где игроки угадывают слова на основе семантической близости.

## 🎮 Описание игры

Semantle - это игра-угадайка, где:
- Загадано одно секретное слово (TARGET)
- Игрок вводит слова (GUESS)
- Система оценивает семантическую близость между GUESS и TARGET
- Чем выше число (0–100%), тем ближе смысл слова к загаданному
- Игра продолжается, пока слово не угадано

## 🚀 Быстрый старт

### Требования

- Python 3.8+
- Node.js 16+
- npm или yarn

### Установка и запуск

#### 🎯 Быстрый запуск (одной командой)

После установки зависимостей (см. ниже), вы можете запустить игру одной командой:

```bash
# Из корневой директории проекта
python start.py
```

Этот скрипт автоматически запустит:
- ✅ Backend сервер на `http://localhost:8000`
- ✅ Frontend сервер на `http://localhost:3000`

Для остановки нажмите `Ctrl+C` в терминале.

#### Установка зависимостей

#### 1. Backend (FastAPI)

```bash
# Перейти в директорию backend
cd backend

# Создать виртуальное окружение (рекомендуется)
python -m venv venv

# Активировать виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Запустить сервер
python main.py
```

Backend будет доступен по адресу: `http://localhost:8000`

#### 2. Frontend (React + Vite)

```bash
# Перейти в директорию frontend
cd frontend

# Установить зависимости
npm install
```

**Примечание:** После установки зависимостей используйте `python start.py` для запуска обеих частей приложения одновременно.

#### Альтернативный запуск (вручную)

Если вы хотите запускать серверы отдельно:

**Backend:**
```bash
cd backend
# Активировать виртуальное окружение
venv\Scripts\activate  # Windows
# или
source venv/bin/activate  # Linux/Mac
python main.py
```

**Frontend:**
```bash
cd frontend
npm run dev
```

Backend будет доступен по адресу: `http://localhost:8000`  
Frontend будет доступен по адресу: `http://localhost:3000`

## 📁 Структура проекта

```
Semantle/
├── backend/
│   ├── main.py                 # FastAPI приложение
│   ├── game_logic.py           # Логика игры
│   ├── embeddings_manager.py   # Управление embeddings
│   ├── requirements.txt        # Python зависимости
│   └── data/                   # Данные (создается автоматически)
│       └── stats.json          # Статистика игр
├── frontend/
│   ├── src/
│   │   ├── components/        # React компоненты
│   │   │   ├── Game.jsx       # Основной компонент игры
│   │   │   ├── Stats.jsx      # Статистика
│   │   │   └── ...
│   │   ├── App.jsx            # Главный компонент
│   │   └── main.jsx           # Точка входа
│   ├── package.json
│   └── vite.config.js
├── words.json                  # Словарь слов
└── README.md
```

## 🔧 API Endpoints

### `POST /api/game/new`
Создать новую игру
```json
{
  "daily": false
}
```

### `POST /api/game/guess`
Сделать попытку угадать слово
```json
{
  "word": "example",
  "session_id": "uuid"
}
```

### `GET /api/game/{session_id}`
Получить информацию о сессии

### `GET /api/stats/{user_id}`
Получить статистику пользователя

### `GET /api/words/validate/{word}`
Проверить, есть ли слово в словаре

## 🎯 Функции

### Основные функции
- ✅ Угадывание слов по семантической близости
- ✅ Отображение оценки близости (0-100%)
- ✅ Ранжирование попыток
- ✅ История попыток с визуализацией

### Продвинутые функции
- ✅ Личный кабинет с историей попыток
- ✅ Статистика угадываний
- ✅ Ежедневное слово (Daily Challenge)
- ✅ Графическое отображение прогресса

## 🧠 Система Embeddings

Игра использует **OpenAI Embeddings** для расчета семантической близости слов.

### Текущая реализация
- **OpenAI Embeddings**: Использует модель `text-embedding-3-small` от OpenAI
- **Кэширование**: Embeddings кэшируются для оптимизации производительности
- **Fallback**: При недоступности API используется резервный метод на основе символов

### Настройка API ключа

Для работы с OpenAI Embeddings необходимо установить API ключ:

**Рекомендуемый способ (через переменную окружения):**
```bash
# Windows PowerShell
$env:OPENAI_API_KEY="your-api-key-here"

# Windows CMD
set OPENAI_API_KEY=your-api-key-here

# Linux/Mac
export OPENAI_API_KEY="your-api-key-here"
```

**Альтернативный способ:**
API ключ можно указать напрямую в коде `embeddings_manager.py` (не рекомендуется для production).

### Преимущества OpenAI Embeddings

- ✅ Высокое качество семантических сравнений
- ✅ Поддержка контекстных значений слов
- ✅ Не требует загрузки больших моделей локально
- ✅ Автоматическое кэширование для оптимизации

### Альтернативные варианты (устарело)

Ранее поддерживались локальные модели:
- Word2Vec (Google News) - требует ~1.6 GB
- GloVe Vectors - требует загрузки и настройки

Эти варианты больше не используются, так как OpenAI Embeddings обеспечивают лучшее качество.

## 🎨 Технологии

### Backend
- **FastAPI** - современный веб-фреймворк
- **Python** - язык программирования
- **NumPy** - численные вычисления
- **Gensim** - работа с word embeddings (опционально)

### Frontend
- **React** - UI библиотека
- **Vite** - сборщик и dev сервер
- **Axios** - HTTP клиент

## 📊 Статистика

Игра автоматически сохраняет статистику:
- Общее количество игр
- Завершенные игры
- Среднее количество попыток
- Лучший результат
- История последних игр

## 🔐 Безопасность

- Валидация входных данных
- Проверка слов на наличие в словаре
- Защита от повторных попыток одного слова
- CORS настройки для безопасности

## 🐛 Решение проблем

### Backend не запускается
- Убедитесь, что Python 3.8+ установлен
- Проверьте, что все зависимости установлены: `pip install -r requirements.txt`
- Проверьте, что порт 8000 свободен

### Frontend не подключается к API
- Убедитесь, что backend запущен на порту 8000
- Проверьте настройки CORS в `backend/main.py`
- Проверьте proxy настройки в `frontend/vite.config.js`

### Низкое качество embeddings
- Используйте предобученные модели (см. раздел "Система Embeddings")
- Убедитесь, что слова из словаря есть в модели

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Вклад

Приветствуются улучшения и предложения!

## 📧 Контакты

Для вопросов и предложений создайте issue в репозитории.

---

**Приятной игры! 🎮**
